Imports System.Collections.Generic

Namespace Models
    Public Class BookRepository
        ' Gestione CRUD per MongoDB
        Public Sub AddBook(book As Book)
            ' Codice per aggiungere un libro al database
        End Sub

        Public Function GetAllBooks() As List(Of Book)
            ' Codice per recuperare tutti i libri dal database
        End Function

        ' Altri metodi per rimuovere, aggiornare, ecc.
    End Class
End Namespace